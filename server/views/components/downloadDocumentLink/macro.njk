

{% macro downloadDocumentLink(crn, documentId, linkText, linkStyle, documentName) -%}
<a class="{{ linkStyle }}" href="/{{crn}}/documents/{{documentId}}/{{documentName}}">{{ linkText }}</a>
{%- endmacro %}