{% extends "partials/layout.njk" %}
{%- from "moj/components/sub-navigation/macro.njk" import mojSubNavigation -%}
{% set pageTitle = applicationName + " - Case view" %}

{% block content %}

    <h1 class="govuk-heading-xl govuk-!-margin-bottom-2">{{ data.name or name}}</h1>
    <span class="govuk-caption-xl">Tier: {{ data.tier or tier }}</span>
    <span class="govuk-caption-xl">CRN: {{ data.crn or crn }}</span>

    {{ mojSubNavigation({
        label: 'Sub navigation',
        items: [
            {
                text: 'Summary',
                href: '/' + crn + '/case-view',
                active: title === 'Summary'
            },
            {
                text: 'Probation record',
                href: '/probation-record',
                active: title === 'Probation record'
            },
            {
                text: 'Risk',
                href: '/risk',
                active: title === 'Risk'
            }
        ]
    }) }}

    {% block caseContent %}{% endblock %}

{% endblock %}

